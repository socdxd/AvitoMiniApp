<Window x:Class="AvitoMiniApp.Views.CompleteAdWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:fa="http://schemas.awesome.incremented/wpf/xaml/fontawesome.sharp"
        Title="Завершение объявления" Height="300" Width="450"
        WindowStartupLocation="CenterScreen"
        Background="#F5F5F5"
        ResizeMode="NoResize">
    <Border Background="White" 
            CornerRadius="12" 
            Margin="30"
            Padding="40">
        <Border.Effect>
            <DropShadowEffect Color="#000000" Opacity="0.1" BlurRadius="20" ShadowDepth="2"/>
        </Border.Effect>

        <StackPanel VerticalAlignment="Center">
            <fa:IconImage Icon="CheckCircle"
                          Width="48"
                          Height="48"
                          Foreground="#00C853"
                          HorizontalAlignment="Center"
                          Margin="0,0,0,20"/>

            <TextBlock Text="Завершение сделки" 
                       FontSize="22" 
                       FontWeight="SemiBold" 
                       HorizontalAlignment="Center"
                       Foreground="#333333"
                       Margin="0,0,0,30"/>

            <TextBlock Text="Введите финальную сумму продажи" 
                       FontSize="14" 
                       Foreground="#666666"
                       Margin="0,0,0,8"/>
            
            <Border Background="#F8F9FA" 
                    CornerRadius="8" 
                    Padding="12" 
                    Margin="0,0,0,30">
                <TextBox x:Name="FinalPriceTextBox" 
                         BorderThickness="0" 
                         Background="Transparent"
                         FontSize="14"
                         VerticalContentAlignment="Center"/>
            </Border>

            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="*"/>
                </Grid.ColumnDefinitions>

                <Button Content="Отмена" 
                        Background="#E0E0E0" 
                        Foreground="#666666"
                        Padding="0,12"
                        FontSize="14"
                        FontWeight="SemiBold"
                        Margin="0,0,8,0"
                        Click="CancelButton_Click">
                    <Button.Style>
                        <Style TargetType="Button">
                            <Setter Property="Template">
                                <Setter.Value>
                                    <ControlTemplate TargetType="Button">
                                        <Border Background="{TemplateBinding Background}"
                                                CornerRadius="8"
                                                Padding="{TemplateBinding Padding}">
                                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                                        </Border>
                                    </ControlTemplate>
                                </Setter.Value>
                            </Setter>
                            <Style.Triggers>
                                <Trigger Property="IsMouseOver" Value="True">
                                    <Setter Property="Background" Value="#BDBDBD"/>
                                </Trigger>
                            </Style.Triggers>
                        </Style>
                    </Button.Style>
                </Button>

                <Button Content="Завершить" 
                        Grid.Column="1"
                        Background="#00C853" 
                        Foreground="White"
                        Padding="0,12"
                        FontSize="14"
                        FontWeight="SemiBold"
                        Margin="8,0,0,0"
                        Click="CompleteButton_Click">
                    <Button.Style>
                        <Style TargetType="Button">
                            <Setter Property="Template">
                                <Setter.Value>
                                    <ControlTemplate TargetType="Button">
                                        <Border Background="{TemplateBinding Background}"
                                                CornerRadius="8"
                                                Padding="{TemplateBinding Padding}">
                                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                                        </Border>
                                    </ControlTemplate>
                                </Setter.Value>
                            </Setter>
                            <Style.Triggers>
                                <Trigger Property="IsMouseOver" Value="True">
                                    <Setter Property="Background" Value="#00A843"/>
                                </Trigger>
                            </Style.Triggers>
                        </Style>
                    </Button.Style>
                </Button>
            </Grid>
        </StackPanel>
    </Border>
</Window>
